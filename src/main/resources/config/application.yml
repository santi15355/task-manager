server:
  port: ${PORT:5000}

base-url: /api

rollbar_token: 8425592a0bc94e2ab85389df2f793ad7

spring:
  application:
    name: taskManager

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
